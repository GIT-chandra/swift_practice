if Int.max == Int32.max {
    print("This is a 32 bit system!")
}

if Int.max == Int64.max {
    print("This is a 64 bit system!")
}

// probably there's some else or elif type of thing in swift too, but haven't reached that part :D